# Instalação

```yarn install```

A aplicação é iniciada em ```src/index.js``` que chama o ```src/App.js```

Para ver a aplicação rodando em ambiente de desenvolvimento: ```yarn electron:serve```
Para fazer o build para produção: ```yarn electron:build```